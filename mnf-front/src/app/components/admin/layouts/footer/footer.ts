import { Component } from '@angular/core';

@Component({
  selector: 'app-admin-footer',
  imports: [],
  templateUrl: './footer.html',
  standalone: true,
  styleUrl: './footer.css'
})
export class Footer {
  // We can add properties or methods here if needed in the future
}
