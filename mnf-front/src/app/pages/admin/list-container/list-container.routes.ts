import { Routes } from '@angular/router';
import { ListContainerComponent } from './list-container';

export const LIST_CONTAINER_ROUTES: Routes = [
  { path: '', component: ListContainerComponent }
];
